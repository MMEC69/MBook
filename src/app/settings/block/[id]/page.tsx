import Settings from "@/features/settings/page_components/Settings";
import React from "react";

export default function page() {
  return (
    <div>
      <Settings type="settingsBlock" />
    </div>
  );
}
